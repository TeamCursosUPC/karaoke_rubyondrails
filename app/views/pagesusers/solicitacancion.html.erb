
<h1>Canciones</h1>
<h1><%= @q %></h1>

<div style = "display:none">
    <%= @idpedido=Request.maximum("id") %>
</div>

<%= bootstrap_form_for(@detailRequestSong) do |f| %>
  <%= f.text_field :request_id, label: "N° Pedido:", value: @idpedido, :readonly => true %>
  <%= f.collection_select :song_id, Song.all, :id, :nombre, label: "Seleccione Cancion:" %>
  <%= f.submit "Hacer pedido", class: "btn btn-success" %>
<% end %>
    <br>
<br>
<br>
<br>

   <h3>Detalle de Pedido Cancion</h3>
        <div class="col-sm-12 col-md-10 col-md-offset-1">
          <table class="table table-hover" cellspacing="0" width="100%">
            <thead>
              <tr >
                <th>#</th>
                <th>Cancion</th>
                <th>Eliminar</th>
              </tr>
            </thead>

            <tbody>
            <% @detailRequestSongAll.each do |detailRequestSong| %>
                <tr>
                  <td><%= detailRequestSong.id %></td>
                  <td><h4 class="media-heading"><%= detailRequestSong.song.nombre %></a></td>
                  <td align="center" class="col-sm-1 col-md-1">
                    <%= link_to(detailRequestSong, method: :delete ) do %>
                        <p data-placement="top" data-toggle="tooltip" title="Delete" >
                          <button class="btn btn-danger" data-title="Delete" data-toggle="modal" >
                            <span class="glyphicon glyphicon-remove"></span>
                          </button>
                        </p>
                    <% end %>
                  </td>
                </tr>
            <% end %>
                
                    <tr>
                        <td>   </td>
                        <td>   </td>
                        <td>   </td>
                        <td>
                        </td>
                        <td>
                        <button type="button" class="btn btn-success">
                            Checkout <span class="glyphicon glyphicon-play"></span>
                        </button></td>
                    </tr>
            </table>
            </tbody>
          </table>

