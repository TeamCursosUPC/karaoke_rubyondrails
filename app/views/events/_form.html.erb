<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre_de_evento %>
    <%= f.text_field :Nombre_de_evento %>
  </div>

  <div class="field">
    <%= f.label :Fecha_y_hora %>
    <%= f.datetime_select :Fecha_y_hora %>
  </div>

  <div class="field">
    <%= f.label :Capacidad_maxima %>
    <%= f.number_field :Capacidad_maxima %>
  </div>

  <div class="field">
    <%= f.label :Descripcion %>
    <%= f.text_field :Descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
